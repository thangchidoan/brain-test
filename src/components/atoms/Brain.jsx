import { Brain as BrainIcon } from 'lucide-react';

export const Brain = ({ size = 48, className = 'text-stone-800' }) => {
  return (
    <BrainIcon size={size} strokeWidth={1} className={className} />
  );
};
